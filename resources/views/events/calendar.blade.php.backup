@extends('layouts.app')

@section('title', 'Events Calendar - Leadership Summit 2025')
@section('meta_description', 'View all Leadership Summit events in calendar format. Plan your schedule and register for sessions.')

@section('breadcrumbs')
<li class="breadcrumb-item"><a href="{{ route('events.index') }}">Events</a></li>
<li class="breadcrumb-item active" aria-current="page">Calendar</li>
@endsection

@push('styles')
<style>
    .calendar-header {
        background: linear-gradient(135deg, var(--primary-color) 0%, #1e3a8a 100%);
        color: white;
        padding: 3rem 0 2rem;
        text-align: center;
    }

    .calendar-header h1 {
        font-size: 2.5rem;
        font-weight: 700;
        margin-bottom: 0.5rem;
    }

    .calendar-controls {
        background: white;
        padding: 1.5rem 0;
        border-bottom: 1px solid #e5e7eb;
        position: sticky;
        top: 70px;
        z-index: 100;
    }

    .month-navigation {
        display: flex;
        justify-content: space-between;
        align-items: center;
        max-width: 400px;
        margin: 0 auto;
    }

    .nav-btn {
        background: var(--primary-color);
        color: white;
        border: none;
        padding: 0.75rem 1rem;
        border-radius: 0.5rem;
        text-decoration: none;
        transition: all 0.2s ease;
        display: flex;
        align-items: center;
        gap: 0.5rem;
    }

    .nav-btn:hover {
        background: var(--secondary-color);
        color: white;
        transform: translateY(-1px);
    }

    .nav-btn:disabled {
        opacity: 0.5;
        cursor: not-allowed;
        transform: none;
    }

    .current-month {
        font-size: 1.5rem;
        font-weight: 600;
        color: var(--primary-color);
    }

    .calendar-container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 2rem 0;
    }

    .calendar-grid {
        display: grid;
        grid-template-columns: repeat(7, 1fr);
        gap: 1px;
        background: #e5e7eb;
        border-radius: 0.5rem;
        overflow: hidden;
        margin-bottom: 2rem;
    }

    .calendar-header-cell {
        background: var(--primary-color);
        color: white;
        padding: 1rem;
        text-align: center;
        font-weight: 600;
        font-size: 0.9rem;
    }

    .calendar-cell {
        background: white;
        min-height: 120px;
        padding: 0.5rem;
        position: relative;
        border: 1px solid transparent;
        transition: all 0.2s ease;
    }

    .calendar-cell:hover {
        background: #f8fafc;
        border-color: var(--primary-color);
    }

    .calendar-cell.other-month {
        background: #f8f9fa;
        color: #6b7280;
    }

    .calendar-cell.today {
        background: linear-gradient(135deg, rgba(var(--primary-color-rgb), 0.1), rgba(var(--secondary-color-rgb), 0.1));
        border-color: var(--primary-color);
    }

    .date-number {
        font-weight: 600;
        font-size: 1.1rem;
        margin-bottom: 0.5rem;
        color: var(--primary-color);
    }

    .calendar-cell.other-month .date-number {
        color: #9ca3af;
    }

    .event-item {
        background: var(--primary-color);
        color: white;
        padding: 0.25rem 0.5rem;
        border-radius: 0.25rem;
        font-size: 0.75rem;
        margin-bottom: 0.25rem;
        cursor: pointer;
        transition: all 0.2s ease;
        overflow: hidden;
        text-overflow: ellipsis;
        white-space: nowrap;
    }

    .event-item:hover {
        background: var(--secondary-color);
        transform: scale(1.02);
    }

    .event-item.featured {
        background: #f59e0b;
    }

    .event-item.free {
        background: #10b981;
    }

    .more-events {
        font-size: 0.7rem;
        color: #6b7280;
        text-align: center;
        margin-top: 0.25rem;
    }

    .events-list {
        background: white;
        border-radius: 0.5rem;
        padding: 2rem;
        box-shadow: 0 4px 20px rgba(0, 0, 0, 0.08);
    }

    .events-list h3 {
        color: var(--primary-color);
        margin-bottom: 1.5rem;
        font-size: 1.5rem;
    }

    .event-list-item {
        display: flex;
        align-items: center;
        padding: 1rem;
        border: 1px solid #e5e7eb;
        border-radius: 0.5rem;
        margin-bottom: 1rem;
        transition: all 0.2s ease;
        text-decoration: none;
        color: inherit;
    }

    .event-list-item:hover {
        border-color: var(--primary-color);
        box-shadow: 0 2px 8px rgba(0, 0, 0, 0.1);
        transform: translateY(-1px);
        color: inherit;
    }

    .event-date {
        background: var(--primary-color);
        color: white;
        padding: 1rem;
        border-radius: 0.5rem;
        text-align: center;
        margin-right: 1rem;
        min-width: 80px;
    }

    .event-date .day {
        font-size: 1.5rem;
        font-weight: bold;
        line-height: 1;
    }

    .event-date .month {
        font-size: 0.8rem;
        opacity: 0.9;
    }

    .event-details {
        flex: 1;
    }

    .event-title {
        font-weight: 600;
        margin-bottom: 0.25rem;
        color: var(--primary-color);
    }

    .event-meta {
        font-size: 0.9rem;
        color: #6b7280;
        display: flex;
        gap: 1rem;
        flex-wrap: wrap;
    }

    .view-toggle {
        display: flex;
        gap: 0.5rem;
        margin-bottom: 2rem;
        justify-content: center;
    }

    .view-btn {
        padding: 0.5rem 1rem;
        border: 2px solid var(--primary-color);
        background: white;
        color: var(--primary-color);
        border-radius: 0.5rem;
        text-decoration: none;
        transition: all 0.2s ease;
    }

    .view-btn.active,
    .view-btn:hover {
        background: var(--primary-color);
        color: white;
    }

    @media (max-width: 768px) {
        .calendar-grid {
            font-size: 0.8rem;
        }

        .calendar-cell {
            min-height: 80px;
            padding: 0.25rem;
        }

        .event-item {
            font-size: 0.65rem;
            padding: 0.125rem 0.25rem;
        }

        .calendar-header h1 {
            font-size: 2rem;
        }

        .month-navigation {
            flex-direction: column;
            gap: 1rem;
        }

        .event-list-item {
            flex-direction: column;
            text-align: center;
        }

        .event-date {
            margin-right: 0;
            margin-bottom: 1rem;
        }
    }
</style>
@endpush

@section('content')
<!-- Calendar Header -->
<section class="calendar-header">
    <div class="container">
        <h1>Events Calendar</h1>
        <p>View all summit events in calendar format</p>
    </div>
</section>

<!-- Calendar Controls -->
<section class="calendar-controls">
    <div class="container">
        <div class="month-navigation">
            <a href="{{ route('events.calendar', ['year' => $prevMonth->year, 'month' => $prevMonth->month]) }}"
                class="nav-btn {{ $prevMonthEvents == 0 ? 'disabled' : '' }}">
                <i class="fas fa-chevron-left" aria-hidden="true"></i>
                {{ $prevMonth->format('M Y') }}
                @if($prevMonthEvents > 0)
                <span class="badge bg-light text-primary ms-1">{{ $prevMonthEvents }}</span>
                @endif
            </a>

            <div class="current-month">
                {{ now()->setYear($year)->setMonth($month)->format('F Y') }}
            </div>

            <a href="{{ route('events.calendar', ['year' => $nextMonth->year, 'month' => $nextMonth->month]) }}"
                class="nav-btn {{ $nextMonthEvents == 0 ? 'disabled' : '' }}">
                {{ $nextMonth->format('M Y') }}
                @if($nextMonthEvents > 0)
                <span class="badge bg-light text-primary ms-1">{{ $nextMonthEvents }}</span>
                @endif
                <i class="fas fa-chevron-right" aria-hidden="true"></i>
            </a>
        </div>
    </div>
</section>

<div class="container">
    <div class="calendar-container">
        <!-- View Toggle -->
        <div class="view-toggle">
            <a href="{{ route('events.calendar', request()->query()) }}" class="view-btn active">
                <i class="fas fa-calendar-alt me-1" aria-hidden="true"></i>Calendar View
            </a>
            <a href="{{ route('events.index') }}" class="view-btn">
                <i class="fas fa-list me-1" aria-hidden="true"></i>List View
            </a>
        </div>

        <!-- Calendar Grid -->
        <div class="calendar-grid">
            <!-- Calendar Headers -->
            <div class="calendar-header-cell">Sun</div>
            <div class="calendar-header-cell">Mon</div>
            <div class="calendar-header-cell">Tue</div>
            <div class="calendar-header-cell">Wed</div>
            <div class="calendar-header-cell">Thu</div>
            <div class="calendar-header-cell">Fri</div>
            <div class="calendar-header-cell">Sat</div>

            @php
            $currentDate = now()->setYear($year)->setMonth($month);
            $startOfMonth = $currentDate->copy()->startOfMonth();
            $endOfMonth = $currentDate->copy()->endOfMonth();
            $startOfCalendar = $startOfMonth->copy()->startOfWeek();
            $endOfCalendar = $endOfMonth->copy()->endOfWeek();

            $calendarDays = [];
            $date = $startOfCalendar->copy();

            while ($date <= $endOfCalendar) {
                $calendarDays[]=$date->copy();
                $date->addDay();
                }
                @endphp

                @foreach($calendarDays as $date)
                @php
                $dayEvents = $events->filter(function($event) use ($date) {
                return $event->start_date->isSameDay($date);
                });

                $isCurrentMonth = $date->month == $month;
                $isToday = $date->isToday();
                @endphp

                <div class="calendar-cell {{ !$isCurrentMonth ? 'other-month' : '' }} {{ $isToday ? 'today' : '' }}">
                    <div class="date-number">{{ $date->day }}</div>

                    @foreach($dayEvents->take(3) as $event)
                    <div class="event-item {{ $event->status === 'featured' ? 'featured' : '' }} {{ $event->tickets->where('price', 0)->count() > 0 ? 'free' : '' }}"
                        onclick="window.location.href='{{ route('events.show', $event->slug) }}';"
                        title="{{ e($event->title) }} - {{ $event->start_date->format('g:i A') }}">
                        {{ Str::limit($event->title, 20) }}
                    </div>
                    @endforeach

                    @if($dayEvents->count() > 3)
                    <div class="more-events">
                        +{{ $dayEvents->count() - 3 }} more
                    </div>
                    @endif
                </div>
                @endforeach
        </div>

        <!-- Events List for Current Month -->
        @if($events->count() > 0)
        <div class="events-list">
            <h3>
                Events in {{ now()->setYear($year)->setMonth($month)->format('F Y') }}
                <span class="badge bg-primary ms-2">{{ $events->count() }}</span>
            </h3>

            @foreach($events->sortBy('start_date') as $event)
            <a href="{{ route('events.show', $event->slug) }}" class="event-list-item">
                <div class="event-date">
                    <div class="day">{{ $event->start_date->format('d') }}</div>
                    <div class="month">{{ $event->start_date->format('M') }}</div>
                </div>

                <div class="event-details">
                    <div class="event-title">{{ $event->title }}</div>
                    <div class="event-meta">
                        <span>
                            <i class="fas fa-clock me-1" aria-hidden="true"></i>
                            {{ $event->start_date->format('g:i A') }}
                        </span>
                        @if($event->location)
                        <span>
                            <i class="fas fa-map-marker-alt me-1" aria-hidden="true"></i>
                            {{ $event->location }}
                        </span>
                        @endif
                        <span>
                            <i class="fas fa-tag me-1" aria-hidden="true"></i>
                            @if($event->tickets->where('price', 0)->count() > 0)
                            Free
                            @else
                            ${{ number_format($event->tickets->min('price') ?? 0, 2) }}
                            @endif
                        </span>
                        @if($event->status === 'featured')
                        <span class="badge bg-warning text-dark">Featured</span>
                        @endif
                    </div>
                </div>
            </a>
            @endforeach
        </div>
        @else
        <div class="events-list text-center py-5">
            <i class="fas fa-calendar-times fa-3x text-muted mb-3" aria-hidden="true"></i>
            <h3>No Events This Month</h3>
            <p class="text-muted mb-4">There are no events scheduled for {{ now()->setYear($year)->setMonth($month)->format('F Y') }}.</p>
            <a href="{{ route('events.index') }}" class="btn btn-primary">
                <i class="fas fa-list me-2" aria-hidden="true"></i>View All Events
            </a>
        </div>
        @endif
    </div>
</div>
@endsection

@push('scripts')
<script>
    document.addEventListener('DOMContentLoaded', function() {
        // Add click handlers for calendar navigation
        document.querySelectorAll('.nav-btn.disabled').forEach(btn => {
            btn.addEventListener('click', function(e) {
                e.preventDefault();
            });
        });

        // Add keyboard navigation
        document.addEventListener('keydown', function(e) {
            if (e.key === 'ArrowLeft') {
                const prevBtn = document.querySelector('.nav-btn:first-child');
                if (prevBtn && !prevBtn.classList.contains('disabled')) {
                    window.location.href = prevBtn.href;
                }
            } else if (e.key === 'ArrowRight') {
                const nextBtn = document.querySelector('.nav-btn:last-child');
                if (nextBtn && !nextBtn.classList.contains('disabled')) {
                    window.location.href = nextBtn.href;
                }
            }
        });

        // Add hover effects for event items
        document.querySelectorAll('.event-item').forEach(item => {
            item.addEventListener('mouseenter', function() {
                this.style.transform = 'scale(1.05)';
            });

            item.addEventListener('mouseleave', function() {
                this.style.transform = 'scale(1)';
            });
        });
    });
</script>
@endpush